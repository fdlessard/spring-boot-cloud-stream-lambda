server:
  port: 8081

management:
  endpoints:
    web:
      exposure:
        include: "*"

logging:
  level.root: INFO
  level.io.fdlessard.codebites.cloud.stream.lambda: DEBUG

spring:
  application:
    name: client-lambda-functional-application
  output:
    ansi:
      enabled: always
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB
  jackson:
    default-property-inclusion: non_null
    time-zone: UTC
    serialization:
      write_dates_as_timestamps: false

  cloud:
    function:
      definition: receiveClient
    stream:
      bindings:
        receiveClient-in-0:
          destination: normalized.fmcrm.clients
          group: 1385-fmcrm-clients-group
          contentType: application/json
      kafka:
        binder:
          brokers: lkc-o5qd9-4rp0l.ca-central-1.aws.glb.confluent.cloud:9092
          configuration:
            security.protocol: SASL_SSL
            sasl.mechanism: PLAIN
            sasl.jaas.config: org.apache.kafka.common.security.plain.PlainLoginModule required username="KYUWVJPOVGOAAAWG" password="LIrodyhTgxfyDwhPqNCqtYs+WYLc5yzbQ7TMlgpqtFTCwxd2WXsmr5qp478PV0rD";